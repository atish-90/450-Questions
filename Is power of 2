#include<bits/stdc++.h>
using namespace std;
int solve(int n) {
  return !(n & (n-1));
}
int main() {
  int n; cin >> n;
  if(solve(n)) {
    cout << "YES" << "\n";
  }
  else {
    cout << "NO" << "\n";
  }
  return 0;
}
